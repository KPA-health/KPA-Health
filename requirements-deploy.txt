# Dependencias mínimas para despliegue en servidor (p. ej. PythonAnywhere).
# Sin Whisper ni pytest: más liviano (el modo de IA en servidor es "nube").
# Instalar con:  pip install -r requirements-deploy.txt
fastapi==0.141.1
uvicorn==0.53.0
python-multipart==0.0.32
pydantic==2.13.5
python-dotenv==1.2.3
PyJWT==2.15.0
pandas==3.0.6
httpx==0.28.1
sqlglot==30.19.0
